# A1 çözümü — Önermeselleştirme

`∀x ∃y R(x, y)` ve D = {a, b}:

- Her x için en az bir y:  
  `(R(a,a) ∨ R(a,b)) ∧ (R(b,a) ∨ R(b,b))`

Atomik önerme sembolleri: `R_aa`, `R_ab`, `R_ba`, `R_bb` → **4** sembol.
